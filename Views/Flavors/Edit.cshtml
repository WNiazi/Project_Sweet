@{
  Layout = "_Layout";
}

@model PierreSweets.Models.Flavor

<h2>Edit</h2>

<h4>Edit this flavor: @Html.DisplayFor(model => model.FlavorName)</h4>

@using (Html.BeginForm())
{
  @Html.HiddenFor(model => model.FlavorId)

  @Html.LabelFor(model => model.FlavorName)
  @Html.EditorFor(model => model.FlavorName, new { @required  = "required"})


  <input type="submit" value="Save" />
}

<p>@Html.ActionLink("Back to flavor", "Index")</p>